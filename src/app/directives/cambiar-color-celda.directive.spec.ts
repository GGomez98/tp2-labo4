import { CambiarColorCeldaDirective } from './cambiar-color-celda.directive';

describe('CambiarColorCeldaDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarColorCeldaDirective();
    expect(directive).toBeTruthy();
  });
});
